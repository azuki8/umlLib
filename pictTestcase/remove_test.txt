多重度:0..1,0..5,3..6
ユニーク:あり,なし
オーダー:あり,なし
現在要素数:下限,下限+1,上限
削除対象含有数:0,1,2

if [多重度] = "0..1"
   then [現在要素数] = "下限" or
        [現在要素数] = "上限";

if [ユニーク] = "あり"
   then [削除対象含有数] = 0 or
        [削除対象含有数] = 1;

if [多重度] = "0..1" and  [現在要素数] = "上限"
   then [削除対象含有数] = 0 or
        [削除対象含有数] = 1;

if [多重度] = "0..5" and [現在要素数] = "下限"
   then [削除対象含有数] = 0;

if [多重度] = "0..5" and [現在要素数] = "下限+1"
   then [削除対象含有数] = 0 or
        [削除対象含有数] = 1;

if [多重度] = "0..1" and [現在要素数] = "下限"
   then [削除対象含有数] = 0;